{
  "installCommand": "npx --yes pnpm@10 install --no-frozen-lockfile --ignore-scripts",
  "buildCommand": "npx pnpm@10 --filter @workspace/admin-panel build",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
