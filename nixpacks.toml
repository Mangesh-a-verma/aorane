[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npx --yes pnpm@10 install --no-frozen-lockfile --ignore-scripts"
]

[phases.build]
cmds = [
  "npx pnpm@10 --filter @workspace/api-server build"
]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"
